in STL containers wherever applicable, insert operation takes an iterator, that iterator points to the position where insertion will occur {if begin given new element will be inserted at the begining, if begin()+1 after 0th element}
	insert(start iterator, length of insertion, val)
in STL containers wherever applicable, erase operation takes takes two iterators, operation start from 1st and ends before 2nd that is excluding last pos. 
